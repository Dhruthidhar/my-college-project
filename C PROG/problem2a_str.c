#include <stdio.h>
#include <string.h>

int main (){
    char str[1000];
    scanf("%s",str);
    int rev = str[::-1];
   // for (int i = strlen(str)-1; i < strlen(str); i++){
        printf(rev);
    
    return 0;

    }
    
